from django.apps import AppConfig


from django.apps import AppConfig

class UsersConfig(AppConfig):
    """
    Этот класс определяет настройки для приложения 'users',
    """
    default_auto_field = "django.db.models.BigAutoField"
    name = "users"
